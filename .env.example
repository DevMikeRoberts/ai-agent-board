# Docker Compose environment variables
# Copy this file to .env and adjust as needed:
#   cp .env.example .env

# --- Database ---
# Set DATABASE_URL to use PostgreSQL. Leave empty/unset to use SQLite (default).
# DATABASE_URL=postgresql://kanban:your_password@localhost:5432/kanban

# --- Host projects ---
# Directory on the host to mount into the server container at /host-projects.
# PROJECTS_DIR=~/projects

# --- Agent models (optional overrides) ---
# COPILOT_MODEL=claude-opus-4-20250514
# CLAUDE_MODEL=claude-opus-4-20250514
# CODEX_MODEL=gpt-5.2-codex
